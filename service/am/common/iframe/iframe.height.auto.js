/*在iframe调用的页面内引用*/
function setIFrameHeight(){
	//window.parent.getElement('iframe[id=xxx]').setStyle('height',document.body.scrollHeight+2);
}
setIFrameHeight();
//window.addEvent('mousedown', setIFrameHeight);//鼠标操作引起dom变化改变高度

